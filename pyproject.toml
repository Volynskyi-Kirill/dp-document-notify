[project]
name = "dp-document-notify"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "playwright>=1.62.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]
